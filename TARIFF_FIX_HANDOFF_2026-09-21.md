# Тарифы: проход 21 сентября 2026

Ветка `fix/pilot-bugs-and-ui`, база Flutter `569c4a7f`, backend `323539c`. Push/deploy не выполнялись.

## Изменено

- Редактор не запускает автосохранение незавершённого нового тарифа сразу после выбора услуги. Ждёт название и положительную цену.
- Сверка с сервером сохраняет новые локальные тарифы, которых ещё нет в ответе, и не дублирует уже созданный тариф с совпадающим содержимым. Серверный список задаёт baseline, несохранённый черновик остаётся dirty.
- На время сохранения заблокированы изменение/добавление тарифов; сохранение работает со снимком списка. Отложенный commit обрабатывается после завершения текущего сохранения.
- Старая отдельная PriceEntrySheet закрывается только после успешного ответа. Вызовов этой формы в текущем приложении не найдено; основной путь — GeneralInfoSection.

## Проверено

- `flutter test --no-pub test/features/equipment/vacuum_tariffs_test.dart`: 5 тестов успешно, включая сохранение нового заполненного черновика и отсутствие дубля после успешного создания.
- `git diff --check`: успешно.
- `flutter analyze --no-pub` для трёх изменённых файлов и теста: No issues found.
- Локальный `GET http://127.0.0.1:4000/equipment/guest` возвращает реальные названия `septic_sewage`, `water_pumping`, `liquid_waste`. В локальной базе label также заполнен.
- DTO backend, PriceEntry.fromJson/toJson и TariffDraft передают/переводят label. CreateBookingScreen использует выбранную ранее Equipment, без отдельной загрузки актуальных тарифов при открытии.
- ADB видит SM_S908B (`R3CT60F7MRV`) и SM_A125F (`R58R60PHZ9E`). Сквозной тест не выполнен, новая сборка на телефоны не установлена.

## Не закрыто

1. «Новый тариф» при бронировании: причина на конкретном телефоне не воспроизведена. Эта строка возникает при пустом label. Локальные данные её не объясняют. Проверить адрес API/версию установленного приложения, ответ именно client/favorites/map-пути и кэш выбранной Equipment. Не подменять название догадкой и не исправлять production-данные вслепую. Скриншот с Данияром и ценами 12000/25000 не совпадает с просмотренными локальными карточками.
2. Проверить редактор на телефоне: новая услуга → цена → потеря фокуса → дождаться сохранения → повторно открыть → клиент видит название. Повторить с задержкой сети и ошибкой API. Тесты утилиты не заменяют этот проход.
3. Проверить частичный успех нескольких операций и повтор после сбоя: создание цены, затем обновление основной карточки выполняются отдельными запросами, атомарность не добавлялась.
4. Список оставшихся уведомлений из отчёта Cursor сохраняется: backend-решения владельца, +N, точный unread, чат-badges, объединение socket/FCM звука, полный двухтелефонный сценарий. Этот проход их не менял.

Локальный запуск в `.vscode/launch.json`: `--dart-define-from-file=.env --dart-define-from-file=.env.local`. Не запускать просто flutter run без локальных define: значения по умолчанию ведут на production.
